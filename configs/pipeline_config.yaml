har:
  target_hz: 20
  pretrain_window_s: 10
  supervised_window_s: 5
  supervised_overlap: 0.5
  channels: [acc_x, acc_y, acc_z, gyr_x, gyr_y, gyr_z]
  drop_labels: [0]

eeg:
  target_hz: 160
  window_s: 4
  runs: [4, 8, 12]
  bandpass_low: 1
  bandpass_high: 40
  notch_hz: 60

ecg:
  target_hz: 100
  train_folds: [1, 2, 3, 4, 5, 6, 7, 8]
  val_fold: 9
  test_fold: 10
